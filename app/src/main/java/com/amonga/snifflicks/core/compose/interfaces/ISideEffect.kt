package com.amonga.snifflicks.core.compose.interfaces

/**
 * this is triggered by a user action or a state change, but does not directly affect the state of the view.
 * this could be a navigation event, a toast message, a network request, or a logging operation
 */
interface ISideEffect
