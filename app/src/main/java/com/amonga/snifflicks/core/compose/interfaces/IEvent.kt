package com.amonga.snifflicks.core.compose.interfaces

/**
 * It represents a user action or an external event that affects the view state.
 * These are handled by the view model, which updates the view state and Side effect accordingly and emits it to the view layer.
 */
interface IEvent
